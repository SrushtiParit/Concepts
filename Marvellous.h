#include<stdio.h>   //compiler specified path

struct demo
{
    int A;
    int B;
};

#define PI 3.14      //this is called as macro

typedef int * IPTR;  //creation of typedef = definition of datatype